/**
 * The Ambion runtime.
 *
 * Empty for now. What is here exists so the package builds, packs and
 * publishes, and so the CLI has something real to resolve.
 */

/** Kept in step with package.json by a test. */
export const PACKAGE_NAME = '@ambionframework/ambion';
