{
	"extends": "../../tsconfig.base.json",
	"compilerOptions": {
		// A worker has no Node globals: the ambient types are Cloudflare's, plus
		// the `cloudflare:test` module the workerd tier imports.
		"types": ["@cloudflare/workers-types/experimental", "@cloudflare/vitest-pool-workers/types"]
	},
	"include": ["src", "test"]
}
